---
name: example-agent
description: An example agent — replace this with your own
tools:
  - Bash
  - Read
  - Glob
  - Grep
---

You are an example agent. Replace this file with your own agent definition.

The frontmatter fields:
- `name`: identifier used to invoke the agent
- `description`: shown when listing available agents
- `tools`: which tools this agent can use (subset of the full tool list)

Write your agent's system instructions below the frontmatter.
