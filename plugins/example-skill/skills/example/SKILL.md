---
description: An example skill — replace this with your own
---

This is a placeholder skill. Replace the contents of this file with your own instructions.

The `description` in the frontmatter is what shows up in `/help` and autocomplete.
Add `disable-model-invocation: true` to the frontmatter if the skill should just inject a prompt without calling a model.
